pipeline {
  agent {
    dockerfile {
      filename 'Dockerfile'
    }

  }
  stages {
    stage('') {
      steps {
        sh 'ls -la'
      }
    }
  }
  environment {
    NODE_ENV = 'DEVELOPMENT'
  }
}